[project]
name = "nodz"
version = "0.1.0"
description = "Ynput's fork of Nodz"
requires-python = ">=3.9"
dependencies = [
    "qtpy",
    "PySide6 ==6.7.1"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["nodz*"]

[tool.setuptools.package-data]
nodz = ["*.json"]
